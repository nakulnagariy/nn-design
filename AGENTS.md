# Agent instructions

This repo's guidance for AI assistants lives in [CLAUDE.md](./CLAUDE.md).
Read it before editing — it covers the build commands, file layout, and the
**token rules** (spacing vs. width scales, and why `--spacing-*` keys must not
go into the Tailwind `@theme` block).
